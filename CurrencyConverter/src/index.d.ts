// we are maintaining this file to structure our app and for our datatypes as well

interface Currency {
  name:string;
  value:number;
  flag:string;
  symbol:string;
}

// the idea is that whenever we create button we can follow this interface and these properties are must to have in that